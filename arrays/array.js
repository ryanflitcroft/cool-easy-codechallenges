/// directions in markdown



export function addOneToEndOfArray(array, newEntry) {
    /// the first function is here as a example but I want you to finish it using the .push() method.
  ///this is just there to to pass the test
    return [1, 2, 3, 4, 5];
}
 